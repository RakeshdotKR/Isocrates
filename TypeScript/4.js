//Type Inference
function add(a, b) {
    return a + b;
}
console.log(add(20, 30));
var numbers = [10, 20, 30, 40];
numbers.push(50);
numbers.forEach(function (num) { return console.log(num * 3); });
function stringUppercase(str) {
    console.log(str.toUpperCase());
}
stringUppercase("isocrates");
